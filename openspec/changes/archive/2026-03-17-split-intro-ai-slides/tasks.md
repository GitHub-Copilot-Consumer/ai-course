## 1. 測試先行（TDD）

- [x] 1.1 在現有測試套件中新增測試：驗證 `site/content/lessons/ch-intro-ai.md` 中（不含 front matter）的 `---` 分隔符恰好出現 11 次
- [x] 1.2 執行測試確認目前為紅燈（檔案尚無 11 個分隔符）

## 2. 實作：插入分隔符

- [x] 2.1 在「Token 與 Context Window」H3 末尾之後、「### 機率生成機制（為何輸出不穩定）」H3 之前插入 `---`
- [x] 2.2 在「溫度參數（Temperature）」段落末尾（`**對開發者的意義：**` 段落後）插入 `---`，取代現有的 `---`（確認現有 `---` 位置是否已在此處）
- [x] 2.3 在 Agent Loop ASCII code block 之後、「### ReAct 模式」H3 之前插入 `---`
- [x] 2.4 在「Memory 類型」表格之後插入 `---`，取代現有的 `---`（確認現有 `---` 位置）
- [x] 2.5 在「**3. Codebase 搜尋**」工具說明段落之後、「### 與一般 Chat 的差異」H3 之前插入 `---`
- [x] 2.6 在「為何 Coding Agent 需要規格」段落之後插入 `---`，取代現有的 `---`
- [x] 2.7 在「模型支援光譜」ASCII code block 之後、「### 功能比較表」H3 之前插入 `---`
- [x] 2.8 在功能比較表之後、「### 觀察重點」H3 之前插入 `---`
- [x] 2.9 在「Subagent 架構差異」表格之後、「**Fallback 能力的重要性**」之前插入 `---`
- [x] 2.10 在「本課程的選擇」段落之後插入 `---`，取代現有的 `---`

## 3. 驗證

- [x] 3.1 執行測試確認綠燈（`---` 分隔符恰好 11 個）
- [x] 3.2 手動確認文章文字內容未被更動（僅新增 `---` 行）
- [x] 3.3 執行 Hugo 建置（`cd site && hugo`），確認無建置錯誤
- [x] 3.4 git commit：`feat(content): split ch-intro-ai into 12 presentation slides`
